{
  "name": "edframework-demo",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/services/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "test": "jest"
  },
  "devDependencies": {
    "ts-jest": "^29.0.0",
    "jest": "^29.0.0"
  }
}